2. Заведём вектор пар (ключ, значение), где ключём будет значение данного нам массива в значением пары будет индекс элемента из массива соответствующего элемента.
   Пусть массив начальный это А а вестор пар В (просто удобнее и мне лень писать длинные слова).
   Походимся по массиву А циклом и если А[i] больше B.back().first то просто пушим (A[i], i) в конец В иначе игнорим.
   Если В пуст то просто закидываем элемент (A[i], i).
   По итогу мы получаем отсортированную подмассив.
   После для если мы хотим для какого-то элемнта 